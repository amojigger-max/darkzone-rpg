# گزارش ارتقای دارک‌زون ۴۱٫۱٫۱

**تاریخ: ۲۰۲۶/۰۹/۰۷ — نسخهٔ کد: 41.1.1**

## وضعیت واقعی تحویل

این بسته ادامهٔ همان سورس و رابط قبلی است. **365 تست آفلاین پاس شده**؛ صفر شکست و صفر خطای آزمون. اجرای نهایی روی Python 3.12.14 و aiogram 3.31.0 و همچنین آزمون روی Python 3.13.14 انجام شد. کامپایل و pyflakes کد اجرایی بدون خطا پایان یافتند.

**نصب زنده انجام نشده است.** از طرف این ارتقا هیچ polling دوم، ریست تولید، پرداخت پاداش روی ربات زنده، یا پیام به گروه صورت نگرفته است. کد برای بازبینی در شاخه‌ای جدا ارائه می‌شود؛ ارسال کد به معنای نصب روی ربات فعال نیست. دسترسی مخزن دوباره بررسی شد: `pull=true` و `push=false`. برای انتشار در مخزن فعلی، حساب `merajsacvj78` برای اعمال مستقیم باید Write داشته باشد؛ مسیر جایگزین، بازبینی و ادغام تغییرات توسط مالک مخزن است.

تمام ۴۹۳ شناسهٔ تجهیزات اصلی حفظ شده‌اند؛ کاتالوگ فعلی ۶۱۴ تجهیز دارد. همهٔ ۳۲ فایل تصویر/دارایی اصلی بایت‌به‌بایت حفظ شده‌اند. دیتابیس‌های واقعی، توکن و PAT داخل بسته نیستند.

## اصلاحات این ادامه نسبت به بستهٔ ۴۱٫۱

### رفع گیرکردن جایزه در انبار پُر

قبلاً رسیدن بعضی تجهیزات به سقف ۹ عدد، تکمیل جایزهٔ نقدی ویژه را هم معلق نگه می‌داشت. اکنون پولِ واجد شرایط یک‌بار پرداخت می‌شود، تجهیزات قابل‌جاگیری تحویل می‌شوند و بقیه در ذخیرهٔ هدایا می‌مانند. ذخیره، قدرت فعال یا پول اضافه تولید نمی‌کند. `/gifts` راه انتقال آن را نشان می‌دهد.

برای آزادکردن جا، حذف اختیاری یک تجهیز با تأیید یک‌بارمصرفِ پنج‌دقیقه‌ای اضافه شده است. مشاهدهٔ فهرست یا صفحهٔ تأیید چیزی حذف نمی‌کند؛ تأیید تکراری، لغوشده، منقضی یا متعلق به کشور/کاربر دیگر بی‌اثر است. اسکورت رزروشده قابل حذف نیست. تست هم‌زمانی هشت درخواست جایزه نیز فقط یک پرداخت و یک ذخیرهٔ تجهیزات ثبت کرد.

### جلوگیری از بازنویسی دادهٔ تازه با فایل قدیمی

بررسی اولیه قبلی به‌تنهایی با مشاهدهٔ Git HEAD، تازگی فایل واقعی دیتابیس را ثابت نمی‌کرد. حالا **قبل از بازکردن SQLite یا اجرای مهاجرت**، محتوای واقعی فایل محلی با SHA نسخهٔ سرور مقایسه می‌شود. WAL موجود، فایل متفاوت/خراب، توکن خواندنی، شاخهٔ حفاظت‌شده و metadata مربوط به مخزن/شاخهٔ دیگر، باعث توقف می‌شوند؛ هیچ فایل یا پیشرفتی خودکار دور ریخته نمی‌شود.

رسید ذخیره نیز با هش محتوای ارسالی بررسی می‌شود؛ پاسخ نامطابق، موفق گزارش نمی‌شود. خروج در مرحلهٔ راه‌اندازی قفل پوشه را آزاد می‌کند. روی سرور با دیسک پایدار، حالت `INLOOP_AUTOSAVE=0` ساده‌تر است؛ حالت ابری به منبع دادهٔ تطبیق‌داده‌شده و توکن نویسنده نیاز دارد.

### مسیر نصب برای مالک، بدون PAT شخصیِ خواندنی

فایل `Apply DarkZone upgrade` اضافه شده و `worldwar-bot` پس از نصب توسط مالک از `github.token` با مجوز `contents: write` استفاده می‌کند. ورودی‌ها، آزمون‌ها و مجوز مقصد قبل از قطع runner قبلی بررسی می‌شوند. ریستِ اختیاری نیازمند شناسهٔ دقیق یک گروه است؛ بدون آن فقط rollout اجرا می‌شود. یک مرجع Git از وضعیت قبل از ریست/پرداخت نگهداری می‌شود و وجه/اعلان ثبت‌شده پیش از اجرای bot ذخیره می‌شوند.

**این workflow فقط تهیه و از نظر ساختار/دستورها آفلاین بررسی شده؛ روی GitHub اجرا نشده است.** این فایل به حساب عامل خارج از workflow دسترسی جدید نمی‌دهد. مالک باید ابتدا سورس را در مخزن نصب کند؛ فعال‌سازی واقعی هنوز انجام نشده است.

## قابلیت‌های حفظ‌شده از ۴۱٫۱

| بخش | رفتار پیاده‌سازی‌شده |
|---|---|
| پایگاه‌ها | سیلوی موشکی، پدافند یکپارچه، فرماندهی مشترک؛ کنار پایگاه‌های زمینی، هوایی، دریایی، رادار، تدارکات و پناهگاه قبلی |
| برق | نیروگاه شهری، تولید مؤثر وابسته به سلامت و سوخت، برق اضطراری، اثر محلی روی پدافند و اثر ملی روی اقتصاد/خرید سنگین |
| پالایشگاه | تبدیل مقدار واقعی نفت خامِ انبار به سوخت؛ بدون تولید قبل از آماده‌شدن، بدون درآمد یا محصول گذشته برای خرید جدید |
| مخزن | ظرفیت محدود شهری؛ ذخیرهٔ شهرهای آزاد در دسترس شبکهٔ ملی، مخزنِ شهر اشغالی خارج از دسترس؛ خسارت فقط همان شهر |
| کشتی‌سازی | ساخت زمان‌دار نفت‌کش با توقف/کندی واقعی در خرابی کارگاه؛ تعمیر کشتی فقط پس از بازگشت به بندر مناسب |
| نفت‌کش | ساحلی، MR، افرامکس، VLCC؛ نام دلخواه، ظرفیت، سلامت، بندر مبدأ، وضعیت ساخت/سفر/توقیف/غرق |
| معاملهٔ حمل | پیشنهاد ۳۰دقیقه‌ای، پذیرش خریدار، خروج واقعی بار، پول امانی، تحویل نسبی و بازپرداخت؛ تکرار دکمه دوباره پول نمی‌گیرد |
| مسیر | پهنه‌های دریایی، پنج گذرگاه کنترل‌شده، مسیر بلند دماغه/آرام در صورت امکان؛ بسته‌شدن بعد از عبور اثر عطف‌به‌ماسبق ندارد |
| عوارض | فقط صندوق کنترل‌کننده؛ آلمان می‌تواند در مسیر مربوط به ایران عوارض بپردازد، ولی صندوق ایران را تصاحب نمی‌کند؛ افزایش تعرفه از سقف پیشنهاد، پذیرش را بدون برداشت رد می‌کند |
| اسکورت | صفر تا سه شناور واقعاً موجود؛ واحدهای رزروشده دوباره وارد موج دیگر نمی‌شوند؛ ارتقا/تعمیر نوعِ درگیر اسکورت تا بازگشت متوقف است |
| حمله به نفت‌کش | فقط دشمن جنگ فعال، با استقرار/برد معتبر؛ بندر کشور ثالث حفاظت دارد؛ حداکثر ۲۰٪ آسیب در یک موج و سهمیهٔ مشترک موشک |
| بازیابی | مأموریت و مسیر و امانت در SQLite؛ بسته‌شدن/بازگشایی، انتظار تخلیه، غرق شدن، بازپرداخت و بازگشت با حالت‌های مستقل |
| رابط | `/energy` و `/fleet`، دکمه‌های شهر و هدف جنگی، ورودی نام ماندگار، صفحه‌بندی پیشنهادها، جلوگیری از خرید ناخواسته با `/menu` هنگام نام‌گذاری |

نام رده‌ها و انواع تأسیسات شناخته‌شده است، اما **ظرفیت‌ها، قیمت‌ها، زمان‌ها و جغرافیا مدل بازی هستند؛ نه مشخصات واقعی نظامی یا راهنمای ناوبری.**

## رفتارهای مهم آزموده‌شده

- آسیب نیروگاه همان شهر، پدافند محلی را کاهش می‌دهد؛ خرابی مخزن تمام ذخیرهٔ کشور را منفجر نمی‌کند.
- کارخانهٔ در حال ساخت تولید ندارد. دقیقه‌بندی انرژی در آزمایشِ پردازش یک‌جا و مراجعهٔ مکرر نتیجهٔ یکسان داد.
- کشور بدون رهبر اقتصاد NPC راه نمی‌اندازد؛ مشاهده یا ری‌استارت، مخزن خالی و نیروگاه خراب را رایگان ترمیم نمی‌کند.
- نخستین حمله کشتی سالم را غرق نمی‌کند، بندر را به‌جای کشتی خراب نمی‌کند و شهر یا کشور را واگذار نمی‌کند.
- زدن کشتی تجاری، چهار موج برتری دریایی لازم برای پیاده‌شدن را جعل نمی‌کند.
- نیروی دریایی آلمان از اطلس به خلیج فارس تله‌پورت نمی‌کند؛ استقرار نزدیک یا پایگاه متحد با اجازه لازم است.
- موشک علیه کشتی همان زمان پرواز، برد، سهمیهٔ ۳/۶/۲۰، مهمات و سوخت را مصرف می‌کند. مزیت تأسیسات در پرتاب ثبت می‌شود و با ساخت وسط پرواز بیشتر نمی‌شود.
- پذیرش اشتباه، تحریم تازه، کمبود بار/جا/سوخت/اسکورت و افزایش تعرفه از بودجه، پیش از برداشت مالی رد می‌شوند.
- وجه امانی و بار و اسکورت با خطای ثبت، با هم rollback می‌شوند. فقط بار تحویل‌شده برای فروشنده پرداخت می‌شود.
- غرق شدن برای مهاجم پول نمی‌سازد؛ امانت پرداخت‌نشده به خریدار برمی‌گردد و خود خریدار کشور ثالث هم تگ می‌شود.
- رسیدن در زمان مقرر با ری‌استارت دیرهنگام به‌اشتباه عدم‌تحویل حساب نمی‌شود. مقصد ازکارافتاده، انتظار و سپس بازپرداخت/بازگشت دارد، نه انتقال آنی کالا به مبدأ.
- مهاجرت جدول پایگاه قدیمی، سلامت و سطح موجود را حفظ می‌کند. reset صریح، دادهٔ انرژی و سفرها را هم پاک می‌کند، بدون انتخاب خودکار کشور.
- تست‌های قبلی تجارت، لغو تحریم، مالکیت تنگه، جنگ مرحله‌ای، سازمان ملل و عملیات سفارشی همچنان پاس‌اند.

## پاداش‌ها، بدون دوباره‌پرداخت

| واجد شرایط | مجموع پاداش نقدی آپدیت | تجهیزات حفظ‌شده |
|---|---:|---|
| همهٔ بازیکنان | ۳۰٬۰۰۰ دلار مجازی | — |
| آیدی تعیین‌شدهٔ آمریکا پس از انتخاب آمریکا | جمعاً ۸۰٬۰۰۰ | پنج تجهیز آمریکایی و بهترین پدافند آمریکایی |
| آیدی مالک ایران پس از انتخاب ایران | جمعاً ۹۰٬۰۰۰ | ده تجهیز پیشرفتهٔ ایرانی |

۳۰هزار همگانی داخل مجموع ۸۰/۹۰هزار است؛ موجودی آغازین ۱۰۰۰ دلار جداست. این مبالغ جایگزین وعدهٔ نقدی پرداخت‌نشدهٔ قبلی‌اند. **ارتقای ۴۱ به ۴۱٫۱ شناسهٔ پرداخت را عوض نمی‌کند و جایزهٔ قبلاً پرداخت‌شده را تکرار نمی‌کند.** حساب بدون کشور، ابتدا انتخاب واجد شرایط می‌کند. ریست همچنان فقط با تأیید صریح و بکاپ، در گروه انتخاب‌شده انجام می‌شود.

## دامنهٔ آزمون نهایی

| ماژول آزمون | تعداد |
|---|---:|
| `tests.test_advanced` | 36 |
| `tests.test_campaign` | 60 |
| `tests.test_database` | 12 |
| `tests.test_delivery` | 21 |
| `tests.test_economy` | 45 |
| `tests.test_energy_fleet` | 42 |
| `tests.test_handlers` | 111 |
| `tests.test_payout_storage` | 13 |
| `tests.test_rewards_reset` | 8 |
| `tests.test_runtime` | 2 |
| `tests.test_save_preflight` | 15 |
| **جمع** | **365** |

ساعتِ آزمون قابل جلو بردن است؛ بررسی جنگ چندروزه، آزمون چندروزه روی سرور زنده نبوده است. aiogram Dispatcher واقعی با نشست انتقال آفلاین استفاده شد، نه ارسال آزمایشی به کاربران تلگرام. آزمون حفظ تگ ۱۰هزار حساب نیز آفلاین بود. جزئیات در `docs/test-results.txt` و `docs/test-environment.json` است.

## نصب، ارسال خلاصه و محدودیت‌ها

- `README.md` راهنمای نصب و `tools/rollout.py --all-groups` طرح اجرای همهٔ دنیاهای گروهی ثبت‌شده است؛ پیش‌فرض dry-run و بدون تغییر.
- فقط بعد از نصب کد و توقف runner قبلی، اجرای صریح `--runner-stopped --confirm=APPLY:41.1.1` جایزهٔ واجدان شرایط را اعمال و خلاصهٔ تازه را یک بار در صف هر گروه ثبت می‌کند. `--reset-game` جدا و محدود به همان شناسهٔ تأییدشده است. هیچ گروهی در این کار ریست نشده است.
- `docs/اعلان-همه-گروه‌ها.md` متن آماده است، نه مدرک ارسال. ارسال واقعی را runner پس از نصب از outbox انجام می‌دهد.
- برای rollback، بکاپ کد و دیتابیس باید با هم بازیابی شوند؛ کد قدیمی به‌تنهایی روی دیتای دارای امانت/ناوگان جدید اجرا نشود.
- شبکهٔ تلگرام تحویل exactly-once تضمین نمی‌کند؛ خطای مبهم ارسال به `unknown` می‌رود و بررسی می‌خواهد. دوباره‌پرداخت پول برای جبران خطای ارسال انجام نمی‌شود.
- بازیابی داده‌ای که نسخهٔ قبلی پیش از این کار بازنویسی کرده، بدون بکاپ قدیمی تضمین‌پذیر نیست. نسخهٔ جدید از تکرار برخورد انبار تجارت و سرمایه جلوگیری می‌کند.
- ذخیرهٔ دوره‌ای GitHub جای volume پایدار نیست؛ خاموشی اجباری می‌تواند فاصلهٔ بعد از آخرین autosave را از دسترس خارج کند.
- هیچ ادعایی مبنی بر «صفر باگ در تمام محیط‌ها» یا نصب موفق زنده نشده است؛ آغاز production باید با نظارت لاگ و بکاپ باشد.
