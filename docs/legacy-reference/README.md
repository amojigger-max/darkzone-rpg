# Historical reference only

These are the retained test scripts and v23 report from the original repository.
They are **not the acceptance tests or deployment instructions for v41**. Some assert the obsolete NPC, instant-conquest, early-missile and automatic-reset behavior that was intentionally fixed.

Use `python -m pytest -q` in the project root. The new test entry points retain the old script names. Historical claims about live deployment or being error-free are not new verification.
